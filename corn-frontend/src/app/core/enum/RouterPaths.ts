export enum RouterPaths {
    CURRENT_PATH = "",

    HOME_PATH = "home",
    HOME_DIRECT_PATH = "/home",
    
    BOARDS_PATH = "boards",
    PROJECT_LIST_PATH = "projects",
    UNKNOWN_PATH = "**"
}
